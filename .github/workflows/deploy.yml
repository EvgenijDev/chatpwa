name: Auto Deploy via SSH

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Upload files via SCP (SFTP)
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: 22
          source: "./*,!.git/**,!.github/**,!node_modules/**,!web/node_modules/**"
          target: /var/www/chatpwa.ru

      - name: Build project on remote server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: 22
          script: |
            cd /var/www/chatpwa.ru/web
            npm install --legacy-peer-deps
            npm run build

      # - name: Restart server
      #   uses: appleboy/ssh-action@v0.1.10
      #   with:
      #     host: ${{ secrets.FTP_HOST }}
      #     username: ${{ secrets.FTP_USERNAME }}
      #     key: ${{ secrets.DEPLOY_SSH_KEY }}
      #     port: 22
      #     script: |
      #       cd /var/www/chatpwa.ru
      #       pm2 describe chatpwa > /dev/null 2>&1 && pm2 restart chatpwa || pm2 start server/server.js --name chatpwa
